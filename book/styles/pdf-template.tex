% ============================================================
% The AI Species — LaTeX Template for Print PDF
% ============================================================

% Pandoc includes
$if(highlighting-macros)$
$highlighting-macros$
$endif$

\documentclass[$if(fontsize)$$fontsize$$else$11pt$endif$,a5paper,twoside]{book}

% --- Packages ---
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[ngerman]{babel}
\usepackage{geometry}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage[breakable,skins]{tcolorbox}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{titletoc}
\usepackage{setspace}
\usepackage{microtype}
\usepackage{hyperref}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{enumitem}
\usepackage{float}
\usepackage{caption}
\captionsetup{font={footnotesize,it}, labelfont={footnotesize,bf,it}, format=plain, justification=centering}
\let\origfigure\figure
\let\endorigfigure\endfigure
\renewenvironment{figure}[1][htbp]{%
  \origfigure[H]
}{%
  \endorigfigure
}

% --- Page Geometry (A5 for print) ---
\geometry{
    paperwidth=5.5in,
    paperheight=8.5in,
    inner=0.75in,
    outer=0.5in,
    top=0.7in,
    bottom=0.7in,
}

% --- Colors ---
\definecolor{chapterblue}{HTML}{1a3a5c}
\definecolor{sectionblue}{HTML}{2c5282}
\definecolor{infoboxbg}{HTML}{edf2f7}
\definecolor{infoboxborder}{HTML}{2c5282}
\definecolor{scenebg}{HTML}{fffff0}
\definecolor{sceneborder}{HTML}{d4a574}
\definecolor{quoteborder}{HTML}{2c5282}

% --- Fonts ---
\usepackage{fontspec}
\setmainfont{DejaVu Serif}
\setsansfont{DejaVu Sans}
\setmonofont{DejaVu Sans Mono}

% --- Line spacing ---
\setstretch{1.15}

% --- Paragraph style: no indent, spacing between paragraphs ---
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.5em}

% --- Chapter and Section Formatting ---
% H1 = \chapter (TEIL I-IV)
% H2 = \section (Kapitel 1-20) — with page break!
% H3 = \subsection (Abschnitte)

\titleformat{\chapter}[display]
    {\normalfont\Large\scshape\color{chapterblue}\centering}
    {}
    {0pt}
    {}
\titlespacing*{\chapter}{0pt}{40pt}{30pt}

\titleformat{\section}[display]
    {\normalfont\huge\bfseries\color{chapterblue}}
    {}
    {0pt}
    {}
\titlespacing*{\section}{0pt}{0pt}{20pt}

% Page break before every \section (= Kapitel)
\newcommand{\sectionbreak}{\clearpage}

\titleformat{\subsection}
    {\normalfont\large\bfseries\color{sectionblue}}
    {}
    {0em}
    {}

\titleformat{\subsubsection}
    {\normalfont\normalsize\bfseries\itshape}
    {}
    {0em}
    {}

% --- Infobox Environment ---
\newtcolorbox{infobox}[1][]{
    colback=infoboxbg,
    colframe=infoboxborder,
    coltitle=sectionblue,
    fonttitle=\bfseries\sffamily,
    colbacktitle=white,
    left=8pt,
    right=8pt,
    top=6pt,
    bottom=6pt,
    boxrule=1pt,
    arc=2pt,
    toptitle=4pt,
    bottomtitle=4pt,
    breakable,
    title={#1}
}

% --- Scene Environment ---
\newtcolorbox{szene}{
    colback=scenebg,
    colframe=sceneborder,
    left=12pt,
    right=12pt,
    top=8pt,
    bottom=8pt,
    boxrule=0pt,
    toprule=2pt,
    bottomrule=2pt,
    arc=0pt,
    breakable,
    fontupper=\itshape\color{black!80}
}

% --- Headers and Footers ---
\pagestyle{fancy}
\fancyhf{}
\fancyhead[LE]{\small\itshape The AI Species}
\fancyhead[RO]{\makebox[\headwidth][r]{\small\itshape\truncate{0.85\headwidth}{\nouppercase{\rightmark}}}}
\fancyfoot[C]{\small\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\usepackage{truncate}

% --- Hyphenation and overflow prevention ---
\tolerance=1000
\emergencystretch=1em
\sloppy

% --- No section numbering, but keep marks for headers ---
\setcounter{secnumdepth}{-1}
\renewcommand{\chaptermark}[1]{\markboth{#1}{}}
\renewcommand{\sectionmark}[1]{\markright{#1}}

% --- Hyperlinks ---
\hypersetup{
    colorlinks=true,
    linkcolor=chapterblue,
    urlcolor=sectionblue,
    citecolor=chapterblue,
}
% URL line breaks
\PassOptionsToPackage{hyphens}{url}

% --- Images ---
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\makeatother
\let\oldincludegraphics\includegraphics
\renewcommand{\includegraphics}[2][]{\oldincludegraphics[width=\maxwidth,#1]{#2}}

% --- Tight list (Pandoc compatibility) ---
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% --- Document Info ---
$if(title)$
\title{$title$}
$endif$
$if(author)$
\author{$for(author)$$author$$sep$ \and $endfor$}
$endif$
\date{$date$}

\begin{document}

\frontmatter
\pagestyle{plain}

$if(title)$
\maketitle
$endif$

$if(toc)$
{
\hypersetup{linkcolor=black}
\setcounter{tocdepth}{2}

% --- TOC Styling: hierarchical sizes ---
% \chapter = TEIL (large, bold, no dots)
\titlecontents{chapter}[0em]
    {\addvspace{1.5em}\large\bfseries\color{chapterblue}}
    {}{}
    {\hfill\contentspage}

% \section = Kapitel (medium, bold)
\titlecontents{section}[0em]
    {\addvspace{0.8em}\normalsize\bfseries}
    {}{}
    {\titlerule*[0.5em]{.}\contentspage}

% \subsection = Abschnitte (small, normal weight, indented)
\titlecontents{subsection}[1.5em]
    {\addvspace{0.1em}\small}
    {}{}
    {\titlerule*[0.5em]{.}\contentspage}

\tableofcontents
}
\newpage
$endif$

\mainmatter
\pagestyle{fancy}
% Reset the marks so "Inhaltsverzeichnis" doesn't persist
\markboth{}{}
$body$

\end{document}
